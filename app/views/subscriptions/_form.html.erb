<%= simple_form_for(@subscription || Subscription.new, :html => {:class => 'form-horizontal'}) do |f| %>
  <%= f.error_notification %>

  <%= f.input :email %>
  <%= f.input :name %>

  <%= f.button :submit, class: 'btn-success' %>
<% end %>
