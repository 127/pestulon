<%= simple_form_for path, :html => {:class => 'form-horizontal', :method => :get, :id => 'limit_form'} do |f| %>
  <%= f.input :limit, :label => t('shared.labels.show'), :collection => limits, selected: (cookies[:limit] || 10), input_html: {:name => :limit, :id => :limit} %>
<% end %>